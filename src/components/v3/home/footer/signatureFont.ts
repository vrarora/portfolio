import { Mrs_Saint_Delafield } from "next/font/google";

/** A signature hand, loaded only where the signature is drawn. */
export const signatureFont = Mrs_Saint_Delafield({ subsets: ["latin"], weight: "400", display: "swap" });
